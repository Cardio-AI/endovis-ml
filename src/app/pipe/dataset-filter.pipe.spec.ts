import { DatasetFilterPipe } from './dataset-filter.pipe';

describe('DatasetFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DatasetFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
